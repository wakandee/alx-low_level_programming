#include "main.h"

/**
  * linear_search - search for value in array
  * n thi
  * @array: pointer to array value
  * @size: size of the array
  * @value: size of the value.
  *
  * Return: the first occurence of value in array.
  */
int linear_search(int *array, size_t size, int value){
  if (size == 0)
  {
    return -1;
  }
}